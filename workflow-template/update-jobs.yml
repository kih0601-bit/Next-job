name: Legacy v1 collector (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy pipeline retired guard
        run: |
          echo "Next Job Legacy Pipeline v1 (old 1-10 stages) is retired as of v136."
          echo "Scheduled institution-site collection is intentionally disabled."
          echo "Use Next Job v2 API/code-first workflows instead."
