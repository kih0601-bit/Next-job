# Next Job — Project Guide

이 문서는 대화 기억에만 의존하지 않고 최신 ZIP 자체가 프로젝트의 목표와 합의를 보존하도록 만드는 기준 문서다. 코드 수정 전 이 문서, `docs/patch-history.md`, `docs/source-status.json`, 최신 Actions 진단 산출물을 함께 확인한다.

## 1. 최우선 목표

1차 목표는 사용자가 울산 공공기관·공기업 채용공고를 실제 취업 준비에 사용할 수 있는 정확한 반응형 웹앱을 완성하는 것이다. 사업화·전국 확장·관리자 기능은 1차 실사용 완성을 지연시키지 않는다.

## 2. 고정 파이프라인

기관 접속 → 목록 추출 → 상세페이지 → 첨부파일 → 문서 분석 → 지원조건 구조화/필터 → `jobs.json` → 실사용 UI.

- 접속 성공은 HTTP 200만이 아니라 실제 채용게시판 도달 검증까지 포함한다.
- 목록 성공은 첫 페이지의 실제 게시글을 정확히 읽는 것을 기본 안정화 기준으로 한다.
- 최종 수집 목표는 Pagination을 포함한 전체 페이지의 유효 공고를 누락 없이 수집하는 것이다.
- 기본 파이프라인 통과는 첨부까지이며, 최종 품질에서는 문서분석·필터·저장까지 검증한다.

## 3. 정확성 원칙

정확성 > 시스템 안정성/낮은 불량률 > 유지보수 효율 > 자동화율/수집량.

Actions 성공 여부만 보지 않는다. Silent Failure(겉으로 정상인데 실제로 틀린 오류)를 항상 확인한다. 특히 전날 대비 공고 수 급변, 페이지 수 변화, 목록↔상세 개수 불일치, 첨부 분석률, 필터 결과 이상치, 공고 누락·오탐·미탐을 점검한다. 불확실한 자동 복구보다 관리자 확인이 우선이다.

## 4. 수정 원칙

- 사용자가 제공한 최신 ZIP이 유일한 코드 기준본이다.
- 이미 정상인 기관을 목적 없이 다시 건드리지 않는다.
- 기관별 실패 원인을 Actions evidence/원본 응답/요청 방식/코드 대조로 확정한 뒤 해당 기관 또는 필요한 최소 공통 기능만 수정한다.
- 추측성 범용 Selector 확대와 fallback 누적을 피한다.
- 외부 일시 장애와 코드 결함을 재실행/진단으로 구분한다.
- 수정 후 Regression(회귀) 여부를 확인한다.
- 모든 브리핑/심화진단은 패치 직접 목적과 별개로 20기관 전체 기능 건강상태(접속→목록→상세→첨부→문서분석→필터→최종 저장), 실행시간, Regression, 공고/페이지 수 급변, 목록↔상세 불일치, 첨부 분석률과 Silent Failure 가능성을 함께 확인한다.

## 5. 현재 구조화 방향

현재는 기관별 정확성 확보가 우선이다. 20기관 기본 파이프라인과 Pagination이 안정화된 뒤 중복 로직 Refactoring(공통화)을 수행하고, 실제로 반복되는 유형만 Template(기관 유형/단계별 재사용 틀)로 승격한다. 성급한 공통화로 정상 기관을 깨뜨리지 않는다.

## 6. 확장 원칙

지역, 학력, 고용형태, 제외조건을 특정 사용자 값으로 하드코딩하지 않는다. 모바일·PC를 별도 앱으로 만들지 않고 모바일 우선 반응형 웹앱을 유지한다. 2차 지역 확장 전에 관리자/워크플로/확장 구조를 재점검한다.

## 7. 필터 원칙

단순 키워드 필터가 아니라 실제 지원 가능성을 판단한다. 지원조건은 학력, 필수 자격증/면허, 경력, 연령, 전공/직무 관련 요건, 지역/근무지, 고용형태, 기타 법정·신분 요건 등으로 구조화하고 required / preferred / unknown 상태를 구분한다. 제목보다 상세본문과 PDF/HWP/HWPX 분석 결과를 우선한다.

## 8. 향후 순서

20/20 기본 파이프라인 안정화 → Pagination 전체 페이지 정확성 검증 → 공통 중복 Refactoring → Template 정리 → 지원조건/필터 검증 → `jobs.json` → 실사용 UI 완성 → 필요 시 지역 확장.

## 9. 운영 안전장치

- `docs/patch-history.md`: 왜 바꿨는지, 확정 원인, 실패했던 접근, 유지할 기존 동작을 기록한다.
- `docs/source-status.json`: 기관별 최신 단계 상태와 evidence를 유지한다.
- Pipeline/Regression 보고서: 전 실행과 비교해 후퇴를 감지한다.
- 이 `PROJECT-GUIDE.md`: 대화에서 확정된 장기 원칙과 로드맵을 보존한다.

중요 합의가 변경되면 코드와 함께 이 문서도 증분 갱신한다.

### 진단 실행 원칙
- 코드/기존 evidence/공개 사이트로 자체 확정 가능한 원인은 Actions 전에 끝까지 자체진단한다.
- Runner의 실제 서버 응답이 필요한 부분만 Actions evidence로 확인한다.
- 서버 contract가 확인되지 않은 상태에서 parameter/method를 추측해 성공처럼 보이게 만들지 않는다.
